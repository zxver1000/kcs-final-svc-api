# .env File

```
PORT=3080

[MicroService]
UserHost=localhost
UserPort=3081

FileServerHost=localhost
FileServerPort=3082

[UserAuth]
JWT_SECRET=secret

[RSA]
RSA_SECRET=rsa_secret

[CORS]
CORS_ORIGIN_LIST=localhost
```
